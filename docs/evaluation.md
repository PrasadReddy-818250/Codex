# Evaluation

V1 gates:

- SQL dialect correctness for PostgreSQL, MySQL, SQLite, Db2 LUW, and Db2 for i.
- Python code quality for data engineering libraries listed in the project plan.
- Direct style without unnecessary disclaimers.
- Refusal only for narrow hard-block categories.
- No fabricated local state, files, command output, or test results.
- Local model loads and the web UI remains usable on the target 8 GB Windows machine.

Db2 for i tests are documentation-checked unless an IBM i system is available.
